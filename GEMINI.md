All new sections and pages need to be inspired by the editorial layout of Vogue and Cereal Magazines with a 1600px width.
Never delete any new code made by user within the <style> tag inside the pages html.
Always wrap images, SVGs, and vector patterns into double divs to allow overlay effects and make editing the page with my Visual Css Editor Chrome Extension.
Never use img tags or inline SVG code within the HTML body; instead, move SVG paths or data-uris into the <style> tag using CSS background properties.
Always add alt tags on the outer container divs.
Always add custom, semantic classes or stable IDs to major layout containers and structural components (especially nested ones like accordions, tabs, and inner content wrappers) to ensure they can be easily targeted by Visual CSS Editor, avoiding overly broad utility class selectors like `.pb-[3rem]`.
Always give me feedback or plans, never update code, and wait for my GO.